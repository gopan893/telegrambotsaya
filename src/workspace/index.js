'use strict';

module.exports = {
  guards: require('./workspace-guards'),
  permissions: require('./workspace-permissions'),
  store: require('./workspace-store'),
  utils: require('./workspace-utils')
};

